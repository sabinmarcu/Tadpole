body, html
	background white

// Defining variables
@import "variables"

body
	width 100%
	height 100%
	font-size 10pt
	font-family "Open Sans", sans-serif
	color #242424
	background black

body:not(.landing)
	@import "aside"
	@import "article"
	@import "loadingscreen"
	@import "cases"
    @import "chromeframe"

body.landing
	@import "landing"

body
	.modal-backdrop
		z-index 9999
	.modal
		z-index 99999